Description: Dinamic Load Balancer API on Top of Nginx, LUA and Redis (openresty)

USAGE:
Build docker image
  docker build -t dmatosl/lb .
  docker run  --name lb --rm -i -p 80:8080 -t dmatosl/lb


ADD New upstream
    curl -h 'Host: foo.bar' http://$(docker-machine ip YOUR_BOX)/v1/api/add?ip=10.0.0.1

REMOVE upstream:
	curl -h 'Host: foo.bar' http://$(docker-machine ip YOUR_BOX)/v1/api/rem?ip=10.0.0.1

LIST upstreams for a given Host:
	curl -h 'Host: foo.bar' http://$(docker-machine ip YOUR_BOX)/v1/api/list

Expected output:
{ "operation_status" : "success" }
